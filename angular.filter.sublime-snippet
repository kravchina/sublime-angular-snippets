<snippet>
    <content><![CDATA[(function() {
    'use strict';

    angular
        .module('${1:module}')
        .filter('${2:filter}', ${2:filter});

    ${2:filter}.\$inject = [${3/(\$(\w+)|\w+)/'$1'/g}];

    function ${2:filter}(${3:dependencies}) {
        return function (${4:params}){
            return ${4:params};
        }
    };

})();
]]></content>
    <tabTrigger>ngfilter</tabTrigger>
    <scope>text.plain, source.js</scope>
</snippet>
